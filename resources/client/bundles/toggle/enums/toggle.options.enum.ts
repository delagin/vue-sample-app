export enum toggleQuantifier {
    some = 'some',
    every = 'every',
}

export enum toggleBehavior {
    regular = 'regular',
    inverse = 'inverse',
}

export enum toggleTransitionMode {
    outIn = 'out-in',
    inOut = 'in-out',
    default = 'default',
}

export enum toggleSlotName {
    default = 'default',
    on = 'on',
    off = 'off',
    content = 'content',
}

export enum toggleActions {
    show = 'show',
    hide = 'hide',
    toggle = 'toggle',
}
